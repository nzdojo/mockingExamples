//depended upon component for the purposes of the class.  Make believe it's the database

function Database() {
}

Database.prototype.WritePerson = function(name, lastname) {
   
};

module.exports = Database;